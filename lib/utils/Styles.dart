import 'package:flutter/material.dart';

class Styles
{
	static const textStyle = TextStyle(
		color: Colors.white,
		fontSize: 20
	);
	
	static Color backgroundColor = Colors.grey[900];
	static Color tileColor = Colors.grey[700];
}
